/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"i.pinimg.com",
			"www.hyundai.com",
			"hyundaimobil.co.id",
			"localhost",
		],
	},
};

export default nextConfig;
